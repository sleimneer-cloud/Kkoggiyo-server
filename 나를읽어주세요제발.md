# 이미지 서버 실행
cd img-server
python -m venv venv
source venv/bin/activate
pip install fastapi uvicorn aiomysql python-multipart
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# TCP 서버 빌드 (다른 터미널)
cd tcp-server
mkdir build && cd build
cmake .. && make
./server


#수정 사항 
[3.24 11:14] 
로그인/회원가입 시 sql injection 방어 로직 추가 
비밀번호 평문 저장/비교 수정 >> bcrypt / Argon2 로 서버에서 별도 해싱 후 저장 
db 자격증명 하드코딩 수정 >> .env파일로 변경



[3.25 10:00]
현재 서버 확인사항
-사장클라(회원가입/로그인/채팅)확인 완료
-고객클라(회원가입/로그인/채팅)확인 완료
-라이더 클라(회원가입/로그인/채팅)확인 완료

bcrypt 회원가입 시 bcrpyt를 이용한 암호화 적용하여 db에 저장 모든 클라에 적용


[3.25 20:30]

adminauth 서비스 추가 : 관리자 계정 전용 로그인 로직
admin handler 추가 : 관리자 계정에서 유저정보 확인하는 로직

[3.27 12:20]
관리자 유저 조회/차단및환불 로직 추가 
가게 오픈/클로즈 로직 추가
로그인 로직 변경 :is_bannded=0인 사람만 로그인 성공 패킷 보냄
1이면 로그인 실패

유저 접속하고 있을때 관리자가 차단하면 해당 유저에게 차단 안내 보내고 강제 추방시키는 로직 추가
