#pragma once

#include "json.hpp"

// MessageRouter가 찾고 있던 클래스 정의
class AdminHandler
{
public:
    AdminHandler() = default;
    ~AdminHandler() = default;

    // 앞으로 구현할 관리자용 핸들러 함수 (예시)
    // void handleAdminCommand(int client_fd, const nlohmann::json& j);
};